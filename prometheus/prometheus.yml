global:
  scrape_interval: 15s # By default, scrape targets every 15 seconds.

scrape_configs:
  - job_name: 'gloup-node-api'
    scrape_interval: 5s
    static_configs:
      - targets: ['app:5678']

